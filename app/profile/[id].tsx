// app/profile/[id].tsx
// Expo-router dynamic route — renders ProfileScreen for any /profile/<uuid>
// This file must live at exactly this path for router.push(`/profile/${id}`) to work.

import ProfileScreen from '../../screens/ProfileScreen'

export default ProfileScreen