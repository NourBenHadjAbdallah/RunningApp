// app/activity/[id].tsx
// Expo-router dynamic route — renders ActivityDetailScreen for any /activity/<uuid>
// This file must live at exactly this path for router.push(`/activity/${id}`) to work.

import ActivityDetailScreen from '../../screens/ActivityDetailScreen'

export default ActivityDetailScreen